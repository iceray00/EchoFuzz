introduction_VFCS = """
接下来，是在智能合约领域方面的讨论。

在智能合约领域中，我想提出一种新的范式，请听我为你讲解。
```
提出“易崩溃的函数调用序列(Vulnerable Function Call Sequence, VFCS)”。

在智能合约执行过程中，交易本质上体现为连续的函数调用。若合约持续稳定执行，这些调用序列可无限延续。但当合约状态累积变化，某个函数调用导致崩溃或异常时，该函数调用即为VFCS的终点，而此前引发状态变化的关键函数可选择性地加入VFCS。

VFCS并非简单的调用顺序，而是状态变化累积至某函数调用时，引发异常或崩溃。简而言之，VFCS定义为：

“函数调用持续改变合约状态，当状态变化累积到一定程度，在某次函数调用时发生异常或崩溃，则形成VFCS。”

需明确：
* VFCS未必包含所有函数调用，仅选取关键函数调用。
* 若VFCS包含全部函数调用，则本质为交易序列细化，无法有效缩减漏洞搜索的状态空间。
```

在现实场景中，当前基于Software Engineering的技术（如符号执行、静态分析和规则驱动的序列生成方法），通常只能跟踪函数间显式的数据依赖关系，却难以识别函数调用之间存在的语义关系。比如，某些函数调用可能在逻辑上互相抵消或中和（offsetting interactions）。两者之间存在着特殊的语义关系——调用可能会抵消之前另一个函数调用对状态变量的影响

那么显然，VFCS具有如下三个关键性质：

1、漏洞触发性（Vulnerability Trigger）：
    VFCS序列的最后一个函数调用必须在特定合约状态下执行，这种状态恰好能够触发智能合约的漏洞。
    
2、链式状态依赖性（State Dependence Chain）：
    VFCS中每个函数都是关键函数，且调用均会修改合约的状态，以满足序列中后续函数的调用前置条件。
    
3、最小性（Minimality）：
    VFCS序列需满足最小化条件，即任何该序列的真子序列都无法触发同一漏洞。换言之，VFCS是触发漏洞的最小化调用序列，去掉序列中的任意调用都会导致漏洞不可被触发。
```
"""


# VFCS_format = """
# ```md
# VFCS:
# 1. Initialization Phase
#     - Identify the key functions of the contract.
#     - Define initial state variables and their values.
#     - Identify and call initialization functions.
#
# 2. Function Call Sequence
#     - For each function call:
#         a. Define input parameters.
#         b. Call the function.
#         c. Capture the state before and after the call.
#         d. Check for state changes.
#
# 3. State Analysis
#     - Analyze the state changes after each function call.
#     - Identify potential vulnerabilities based on state inconsistencies or unexpected changes.
#
# 4. Termination Condition
#     - Define the termination condition for the sequence (e.g., a function call leads to a crash/exception).
#
# 5. Vulnerability Function Call Sequence with State
#     - Possible VFCS with state
# ```
# """

solidity_version_diff = """
## 当前版本有可能收到的攻击：
**Solidity 0.4.x 版本**

- 整数溢出和下溢
- 重入攻击
- 委托调用漏洞
- 强制发送攻击
- 未检查的 send 和 transfer 返回值
- 低级调用失败处理不当
- 函数可见性缺失
- 未初始化的存储指针
- 逻辑错误和语法错误
- 随机数不安全
- 依赖区块属性
- 不安全的外部调用


## 当前版本不可能出现的漏洞：
为了更全面地涵盖各个版本不可能出现的漏洞类型，这里列出每个版本改进或消除的具体漏洞：

### Solidity 0.4.x 版本

**不可能出现的漏洞类型：**
- 未指定可见性：在此版本之后，所有函数都必须指定可见性。
- 不严格的类型转换：0.4.x版本之后，类型转换变得更加严格。

"""
#
# solidity_version_diff_other = """
# ## 各版本有可能收到的攻击：
#
# **Solidity 0.4.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
#
#
# **Solidity 0.5.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
#
#
# **Solidity 0.6.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
# - 断言失败
# - 特定编译器优化错误
#
#
# **Solidity 0.7.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
# - 断言失败
# - 特定编译器优化错误
# - 构造函数错误
# - 函数选择器冲突
#
#
# **Solidity 0.8.x 版本**
#
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
# - 断言失败
# - 特定编译器优化错误
# - 构造函数错误
# - 函数选择器冲突
#
#
#
# ## 各版本不可能出现的漏洞：
#
# 为了更全面地涵盖各个版本不可能出现的漏洞类型，这里列出每个版本改进或消除的具体漏洞：
#
# ### Solidity 0.4.x 版本
#
# **不可能出现的漏洞类型：**
# - 未指定可见性：在此版本之后，所有函数都必须指定可见性。
# - 不严格的类型转换：0.4.x版本之后，类型转换变得更加严格。
#
#
# ### Solidity 0.5.x 版本
#
# **不可能出现的漏洞类型：**
# - 函数未声明可见性：此版本强制要求所有函数声明可见性。
# - 字符串与字节数组隐式转换：0.5.x版本之后，字符串和字节数组之间的隐式转换被禁止。
# - 不严格的 abi.encode 和 abi.encodePacked：在此版本中，编码变得更加严格，减少了潜在的编码错误。
#
#
# ### Solidity 0.6.x 版本
#
# **不可能出现的漏洞类型：**
# - 未声明的 receive 函数：从0.6.x版本开始，receive 函数用于接收纯以太币转账。
# - 隐式 fallback 函数：fallback 函数必须显式声明。
# - 不严格的事件签名：事件签名变得更加严格，减少了事件处理错误。
# - 全局变量不安全使用：此版本之后，全局变量的使用更加严格和安全。
#
#
# ### Solidity 0.7.x 版本
#
# **不可能出现的漏洞类型：**
# - 隐式构造函数：此版本要求所有构造函数必须显式声明。
# - 非显式存储指针赋值：在0.7.x中，存储指针赋值变得更加严格。
# - 未初始化的状态变量：此版本之后，编译器更严格地检查未初始化的状态变量。
# - 非显式 pragma 版本声明：强制要求显式声明 pragma 版本，确保编译器版本的一致性。
#
#
# ### Solidity 0.8.x 版本
#
# **不可能出现的漏洞类型：**
# - 整数溢出和下溢：0.8.x版本默认启用了整数溢出和下溢检查，避免了此类漏洞。
# - 未显式使用 unchecked：整数运算默认启用溢出检查，必须显式使用 unchecked 才会禁用溢出检查。
# - 不严格的类型转换：类型转换严格检查，防止隐式转换错误。
# - 未声明的 receive 函数：接收纯以太币转账时，必须显式声明 receive 函数。
# - 未声明的 fallback 函数：必须显式声明 fallback 函数。
# - 不严格的事件签名：事件签名严格检查，减少事件处理错误。
# - 全局变量不安全使用：全局变量使用严格检查，确保安全。
# - 隐式构造函数：所有构造函数必须显式声明，避免隐式行为。
# - 未初始化的状态变量：编译器严格检查未初始化的状态变量，确保状态一致性。
# - 非显式 pragma 版本声明：必须显式声明 pragma 版本，确保编译器版本一致。
#
# """
