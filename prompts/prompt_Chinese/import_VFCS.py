introduction_VFCS = """
接下来，是在智能合约领域方面的讨论。

在智能合约领域中，我想提出一种新的范式，请听我为你讲解。
你可以先看看这篇paper：(Usenix Security'21)SMARTEST Effectively Hunting Vulnerable Transaction Sequences in Smart Contracts through Language Model-Guided Symbolic Execution. 如果你查询不到也没关系，我给你粗略的讲解一下。
这篇paper中（SmarTest），提出了“易崩溃的交易序列(Vulnerable Transaction Sequence, VTS)”。定义为“不断输入的交易序列，到某一个交易时发生崩溃或者异常行为，则以该交易作为VTS的最后一个交易，VTS的其他部分就是导致合约状态改变的其他交易。这样一系列、能改变合约状态而最后导致合约发生崩溃的交易序列，就为VTS”。

请注意，这个VTS是带状态的。我受到VTS的启发，下面是我所想要提出的VFCS：
```
提出“易崩溃的函数调用序列(Vulnerable Function Call Sequence, VFCS)”。

背景：从智能合约角度，不断输入需要进行单笔交易，成为连续的交易序列。从智能合约源代码的角度，其实就是不断的调用函数——不管是单笔交易、还是多笔交易，在合约内部只会反映为不断进行的函数调用，所组成一组函数调用序列。如果合约一直在执行交易且无发生崩溃，那么这个函数调用队列可以看作是无限长度的。这是提出易崩溃的函数调用序列的基石，能将前后进行的交易转化为函数之间的不断调用关系。

这里所说的“函数调用序列”并不是函数相互之间的调用，而是合约进行函数的调用，也就是交易执行过程中合约对函数的使用。在这里提出的“易崩溃的函数调用序列(Vulnerable Function Call Sequence, VFCS)”是指：在合约源代码中函数的层面，不管是单笔交易还是多笔交易都是不断的调用函数，那么如果发生了崩溃、或者异常情况，反映在函数层面，也只可能是在某一个函数发生或在同一时刻某几个函数之中发生，且以最后一个发生崩溃的Function Call作为VFCS的结尾，此前调用的其他改变了合约状态的函数选择性的加入到VFCS中。

同样，这里的VFCS与SmarTest中的VTS一样，都是带有状态的，并不是单纯的函数调用顺序，而是调用到某一个Function时，因其带有某个值，且前面其他函数调用所改变合约的状态，导致了当前这个带状态的函数调用发生了崩溃或者异常行为。

对VFCS做一个High Level的定义：不断进行的函数调用在不断地改变合约的状态，不断累积下，以在某个时刻在某一个函数中发生了崩溃/异常行为作为the End of VFCS。
（确实有点类似于SmarTest的易崩溃的交易序列(Vulnerable Transaction Sequence)的定义，不过这里更细化，是函数层面的，而且考虑压缩状态空间）

直观的说，在不断的交易中，如果触发了这样的VFCS，就说明当前的交易序列是易崩溃(Vulnerable)的。

这里需要强调一个点：这里所说的函数调用序列，并不一定是完整的、所有的函数调用所涉及到的函数都是涵盖到的。

要明确的一点是：（这是一种极端情况）若是全部的函数调用都在这个“易崩溃函数调用序列(VFCS)”中，那么其实就是具体交易序列的变种而已：仅仅是交易序列的细化形式表示，并不能缩减目标漏洞搜寻时的状态空间。相对的，这样进行fuzzing所需要快照模板确定的地方也会少很多，仅需要参数值就可以了。

那么如果希望这个VFCS中的函数序列只是关键函数带易崩溃状态的调用序列，这样能让目标搜寻时的状态空间大大缩减，相对的，这里所需要进行快照模版的定义就会相对复杂，fuzzing多需要考量的方面也会更多。

可以说，这是一个"Trade-Off"。因为最后影响所设计的这个工具的准确性和速度效率，就是前后两部分：从合约中提取VFCS；对VFCS进行快照fuzzing以寻找是否能触发其崩溃的漏洞。
```
"""


# VFCS_format = """
# ```md
# VFCS:
# 1. Initialization Phase
#     - Identify the key functions of the contract.
#     - Define initial state variables and their values.
#     - Identify and call initialization functions.
#
# 2. Function Call Sequence
#     - For each function call:
#         a. Define input parameters.
#         b. Call the function.
#         c. Capture the state before and after the call.
#         d. Check for state changes.
#
# 3. State Analysis
#     - Analyze the state changes after each function call.
#     - Identify potential vulnerabilities based on state inconsistencies or unexpected changes.
#
# 4. Termination Condition
#     - Define the termination condition for the sequence (e.g., a function call leads to a crash/exception).
#
# 5. Vulnerability Function Call Sequence with State
#     - Possible VFCS with state
# ```
# """

solidity_version_diff = """
## 当前版本有可能收到的攻击：
**Solidity 0.4.x 版本**

- 整数溢出和下溢
- 重入攻击
- 委托调用漏洞
- 强制发送攻击
- 未检查的 send 和 transfer 返回值
- 低级调用失败处理不当
- 函数可见性缺失
- 未初始化的存储指针
- 逻辑错误和语法错误
- 随机数不安全
- 依赖区块属性
- 不安全的外部调用


## 当前版本不可能出现的漏洞：
为了更全面地涵盖各个版本不可能出现的漏洞类型，这里列出每个版本改进或消除的具体漏洞：

### Solidity 0.4.x 版本

**不可能出现的漏洞类型：**
- 未指定可见性：在此版本之后，所有函数都必须指定可见性。
- 不严格的类型转换：0.4.x版本之后，类型转换变得更加严格。

"""
#
# solidity_version_diff_other = """
# ## 各版本有可能收到的攻击：
#
# **Solidity 0.4.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
#
#
# **Solidity 0.5.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
#
#
# **Solidity 0.6.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
# - 断言失败
# - 特定编译器优化错误
#
#
# **Solidity 0.7.x 版本**
#
# - 整数溢出和下溢
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
# - 断言失败
# - 特定编译器优化错误
# - 构造函数错误
# - 函数选择器冲突
#
#
# **Solidity 0.8.x 版本**
#
# - 重入攻击
# - 委托调用漏洞
# - 强制发送攻击
# - 未检查的 send 和 transfer 返回值
# - 低级调用失败处理不当
# - 函数可见性缺失
# - 未初始化的存储指针
# - 逻辑错误和语法错误
# - 随机数不安全
# - 依赖区块属性
# - 不安全的外部调用
# - 字符串处理错误
# - 事件处理错误
# - 断言失败
# - 特定编译器优化错误
# - 构造函数错误
# - 函数选择器冲突
#
#
#
# ## 各版本不可能出现的漏洞：
#
# 为了更全面地涵盖各个版本不可能出现的漏洞类型，这里列出每个版本改进或消除的具体漏洞：
#
# ### Solidity 0.4.x 版本
#
# **不可能出现的漏洞类型：**
# - 未指定可见性：在此版本之后，所有函数都必须指定可见性。
# - 不严格的类型转换：0.4.x版本之后，类型转换变得更加严格。
#
#
# ### Solidity 0.5.x 版本
#
# **不可能出现的漏洞类型：**
# - 函数未声明可见性：此版本强制要求所有函数声明可见性。
# - 字符串与字节数组隐式转换：0.5.x版本之后，字符串和字节数组之间的隐式转换被禁止。
# - 不严格的 abi.encode 和 abi.encodePacked：在此版本中，编码变得更加严格，减少了潜在的编码错误。
#
#
# ### Solidity 0.6.x 版本
#
# **不可能出现的漏洞类型：**
# - 未声明的 receive 函数：从0.6.x版本开始，receive 函数用于接收纯以太币转账。
# - 隐式 fallback 函数：fallback 函数必须显式声明。
# - 不严格的事件签名：事件签名变得更加严格，减少了事件处理错误。
# - 全局变量不安全使用：此版本之后，全局变量的使用更加严格和安全。
#
#
# ### Solidity 0.7.x 版本
#
# **不可能出现的漏洞类型：**
# - 隐式构造函数：此版本要求所有构造函数必须显式声明。
# - 非显式存储指针赋值：在0.7.x中，存储指针赋值变得更加严格。
# - 未初始化的状态变量：此版本之后，编译器更严格地检查未初始化的状态变量。
# - 非显式 pragma 版本声明：强制要求显式声明 pragma 版本，确保编译器版本的一致性。
#
#
# ### Solidity 0.8.x 版本
#
# **不可能出现的漏洞类型：**
# - 整数溢出和下溢：0.8.x版本默认启用了整数溢出和下溢检查，避免了此类漏洞。
# - 未显式使用 unchecked：整数运算默认启用溢出检查，必须显式使用 unchecked 才会禁用溢出检查。
# - 不严格的类型转换：类型转换严格检查，防止隐式转换错误。
# - 未声明的 receive 函数：接收纯以太币转账时，必须显式声明 receive 函数。
# - 未声明的 fallback 函数：必须显式声明 fallback 函数。
# - 不严格的事件签名：事件签名严格检查，减少事件处理错误。
# - 全局变量不安全使用：全局变量使用严格检查，确保安全。
# - 隐式构造函数：所有构造函数必须显式声明，避免隐式行为。
# - 未初始化的状态变量：编译器严格检查未初始化的状态变量，确保状态一致性。
# - 非显式 pragma 版本声明：必须显式声明 pragma 版本，确保编译器版本一致。
#
# """
